
@{
    ViewData["Title"] = "Acceso Denegado";
    Boolean hayReturnUrl = !string.IsNullOrEmpty(ViewBag.ReturnUrl);

}
<div class="accesoDenegado">
    <h2>Acceso Denegado</h2>
    <p>No tienes permiso para acceder a esta p��gina.</p>
    @if (hayReturnUrl)
    {
        <p>Por favor, <a href="@ViewBag.ReturnUrl">haz clic aqu��</a> para volver a la p��gina anterior.</p>
    }
    else
    {
        <p><a href="/">Volver al inicio</a></p>
    }
  
</div>
